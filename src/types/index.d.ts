type AuthType = { email: string; password: string; password_confirmation?: string; voucher_code?: string };

type ProfileType = {
  id: any;
  email: string;
  name: string;
  voucher_code: string;
};
